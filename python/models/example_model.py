# Input parameters for example_model.so
#
# These values are made available to the C++ model before or during initialization.
# The parameter names here should mirror the inputs expected by the C++ model.

params = {
}
